lefthook: pixi run --no-progress --environment=lint --frozen lefthook

templates:
  run: run --quiet --no-progress --environment=lint --frozen

colors: true

output:
  - meta # Print lefthook version
  - summary # Print summary block (successful and failed steps)
  - empty_summary # Print summary heading when there are no steps to run
  # - success # Print successful steps
  # - failure # Print failed steps printing
  # - execution # Print any execution logs
  # - execution_out # Print execution output
  # - execution_info # Print `EXECUTE > ...` logging
  - skips # Print "skip" (i.e. no files matched)

pre-commit:
  parallel: true
  jobs:
    - name: taplo
      glob: "*.toml"
      stage_fixed: true
      run: pixi {run} taplo {staged_files}

    - name: prettier
      glob: "*.{css,scss,html,js,json,jsonc,md,yaml,yml}"
      group:
        piped: true
        jobs:
          - name: prettier check
            run: pixi {run} prettier-check {staged_files}
            stage_fixed: true
          - name: prettier format
            run: pixi {run} prettier-format {staged_files}
            stage_fixed: true

    - name: eslint
      glob: "*.{js,jsx,ts,tsx}"
      stage_fixed: true
      run: pixi {run} eslint {staged_files}

    - name: actionlint
      glob: ".github/workflows/*.{yaml,yml}"
      run: pixi {run} actionlint {staged_files}
